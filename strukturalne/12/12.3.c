#include <stdio.h>
#include <string.h>

#define LINE_WIDTH 80
void PrintCentered(const char* str, unsigned length);

int main(void) {


    return 0;
}

void PrintCentered(const char* str, unsigned length) {
    unsigned index = 0;
    while (index != length) {
        
    }
}